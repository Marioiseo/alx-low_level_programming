#include <stdio.h>
/**
 * main -  easy project args are fun
 * @argc: its the counter of argv
 * @argv: its the array of strings or arguments provided
 * Return: 0 and more zeroos
*/
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%i\n", argc);
	return (0);
}
