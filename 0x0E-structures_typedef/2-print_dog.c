#include <stdio.h>
#include <stdlib.h>
#include "dog.h"
/**
 * print_dog - thats cute
 * @d: struct
 */
void print_dog(struct dog *d)
{
	if (d)
	{
	if (d->name == NULL)
		p->name = nil;
	if (d->age == NULL)
		p->age = nil;
	if (d->owner == NULL)
		p->owner = nil;
	printf("name: %s\nAge: %d\nOwner: %s\n", d->name, d->age, d->owner)
	}
}
