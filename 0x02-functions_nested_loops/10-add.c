#include "main.h"
/**
 * add - its easy for me to add ints
 * @a: first num
 * @b: second num
 * Return: c because i am always right ^_^
*/
int add(int a, int b)
{
	int c;

	c = (a + b);
	return (c);
}
