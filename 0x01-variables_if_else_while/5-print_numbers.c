#include <stdio.h>
/**
 * main - nums from 0 to 9
 * Return: "0" success
*/
int main(void)
        {
        int c;

        for(c = 1; c <= 9; c++)
        printf("%d", c);
        return (0);
        }
