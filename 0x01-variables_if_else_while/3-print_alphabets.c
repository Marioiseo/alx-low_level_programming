#include <stdio.h>
/**
 * main - alphabets in chars both 
 * Return: "0" success
*/
int main(void)
        {
        char c;

        for(c = 'a'; c <= 'Z'; c++)
        putchar(c);
        return (0);
        }
