#include <stdio.h>
/**
 * main - alphabets in chars
 * Return: "0" success
*/
int main(void)
        {
        int c;

        for (c = 0 ; c <= 9; c++);
        putchar('0' + c);
	putchar ('\n');
        return (0);
        }
