#include <stdio.h>
/**
 * main - alphabets in chars
 * Return: "0" success
*/
int main(void)
        {
        int c;

        for (c = 1 ; c <= 9; c++);
        putchar(48 + c);
	putchar ('\n');
        return (0);
        }
