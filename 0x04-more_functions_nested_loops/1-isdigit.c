#include "main.h"
/**
 * _isdigit - checking nums not chars
 * @c: the checker again ^_^
 * Return: 0 or 1 as useual <3
*/
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
		return (1);
	else
		return (0);
}
